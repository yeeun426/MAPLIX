// src/pages/WriteStamp.js
import Navbar from '../components/Navbar';
import Layout from '../components/Layout';
import CommunitySideBar from './CommunitySidebar';
import styles from '../components/Community.module.css';


import { useEffect, useState} from 'react';
import { useNavigate, useParams, Link } from "react-router-dom";
import axios from 'axios';


const initialState = {
  m_type: "",
  media_name: "",
};

const initialRecords = {
  record_image1: "",
  record_title1: "",
  record_content1: "",
  record_image2: "",
  record_title2: "",
  record_content2: "",
  record_image3: "",
  record_title3: "",
  record_content3: "",
  record_image4: "",
  record_title4: "",
  record_content4: "",
};

const initialRecord = {
  record_image: "",
  record_title: "",
  record_content: "",
};

const WriteStamp = () => {
  const id = window.localStorage.getItem("id");

  const [state, setState] = useState(initialState);
  const {m_type, media_name} = state;

  const [records, setRecords] = useState(initialRecords);
  const {record_image1, record_title1, record_content1, record_image2, record_title2, record_content2, record_image3, record_title3, record_content3, record_image4, record_title4, record_content4} = records;

  const [record, setRecord] = useState(initialRecord);
  const {record_image, record_title, record_content} = record;
  
  return (
    <div style={{marginTop: "100px"}}>
      <form style={{
        margin: "auto",
        padding: "15px",
        maxWidth: "400px",
        alignContent: "center"
      }}
      onSubmit={handleSubmit}
      >
        <label htmlFor='record_title'>Title</label>
        <input
        type="text"
        id="record_title"
        name="record_title"
        placeholder='제목'
        vlaue={recodr_title}
        onChange={handleInputChange}
        />
        <br></br>

        <label htmlFor='record_content'>내용</label>
        <input className={styles.content}
        type="text"
        id="record_content"
        name="record_content"
        placeholder='내용'
        vlaue={record_content}
        onChange={handleInputChange}
        />
        <br></br>

        <input type="file" id="file" accept='image/*' onChange={handleImgChange} multiple={false} />

        <input type="submit" value="등록" />        
      </form>
    </div>
  );
};
export default WriteStamp;